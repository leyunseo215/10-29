import streamlit as st

st.set_page_config(page_title="🌍 지구 키우기", layout="centered")

# ---------------------------
# 타이틀 & 소개
# ---------------------------
st.title("🌍 지구 키우기 🌱💚")
st.markdown("""
이 앱은 환경 오염의 심각성을 보여주고,  
작은 환경 실천으로 지구를 행복하게 만드는 게임입니다! 🎉
""")

# ---------------------------
# 세션 상태 초기화
# ---------------------------
if "score" not in st.session_state:
    st.session_state.score = 0
if "actions" not in st.session_state:
    st.session_state.actions = []

# ---------------------------
# 시작 화면: 환경 문제 시각화
# ---------------------------
st.header("⚠️ 지금 지구 상태")
st.markdown("""
전 세계 환경 오염이 심각합니다! 😢  
- 연간 플라스틱 쓰레기: 3억 톤 🥤  
- 해수면 상승: 연 3.7mm 🌊  
지금 지구는 슬퍼하고 있어요... 💔
""")

# ---------------------------
# 행동 스코어 시스템
# ---------------------------
st.header("🌱 환경 행동으로 지구를 행복하게 해주세요!")

# 행동 버튼과 점수
actions_dict = {
    "분리수거 ♻️ (+5)": 5,
    "텀블러 사용 ☕ (+3)": 3,
    "대중교통 이용 🚌 (+4)": 4,
    "일회용품 줄이기 🛍️ (+5)": 5
}

cols = st.columns(len(actions_dict))
for i, (action, points) in enumerate(actions_dict.items()):
    with cols[i]:
        if st.button(action):
            st.session_state.score += points
            st.session_state.actions.append(action.split(" ")[0])  # 행동 이름만 저장
            st.balloons()  # 행동 시 이모티콘 폭발 🎈

# 스코어 바 & 지구 행복도
st.subheader(f"현재 점수: {st.session_state.score}")
happiness = min(st.session_state.score / 50, 1.0)  # 50점 만점 기준
st.progress(happiness)

# 지구 캐릭터 감정 표현
st.header("🌍 지구 감정")
if happiness < 0.3:
    st.markdown("😢 슬퍼하는 지구...")
elif happiness < 0.7:
    st.markdown("😐 조금 괜찮아진 지구...")
else:
    st.markdown("😁🌿💚 행복한 지구! 지구가 춤춰요! 💃🎉✨")

# ---------------------------
# 실천한 행동 목록
# ---------------------------
st.header("✅ 오늘 실천한 행동")
if st.session_state.actions:
    for i, act in enumerate(st.session_state.actions, 1):
        st.write(f"{i}. {act} ✅")
else:
    st.write("아직 실천한 행동이 없어요. 위 버튼을 눌러 지구를 행복하게 해주세요! 🌱")

# ---------------------------
# 환경 미션
# ---------------------------
st.header("🎯 오늘의 환경 미션")
missions = [
    "플라스틱 컵 1개 줄이기 🥤❌",
    "전기 사용 1시간 줄이기 💡⚡",
    "텀블러로 음료 마시기 ☕🌿",
    "분리수거 철저히 하기 ♻️💚"
]
st.info(f"오늘의 미션: {missions[st.session_state.score % len(missions)]}")
